// Mirror of shared/constants/constants.json. Keep in sync with the canonical
// file and the client mirror (client/scripts/net/net_contract.gd).

export const MAX_MOVE_SPEED = 120.0; // px/s, screen-space
export const MOVE_TOLERANCE = 8.0; // px slack for latency/jitter
export const NET_TICK_RATE = 10; // match loop Hz
export const SPAWN_X = 0.0;
export const SPAWN_Y = 0.0;

export const WORLD_MATCH_MODULE = "world";
export const WORLD_MATCH_LABEL = "world";
